services:
  manager:
    build: .
    container_name: ai-switch
    ports:
      - "8787:8787"
    volumes:
      - ~/.openclaw:/root/.openclaw:ro
      - ~/.ai-switch:/root/.ai-switch
    restart: unless-stopped
